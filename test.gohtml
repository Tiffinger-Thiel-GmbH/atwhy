<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
<script>
    function show(pageID, title) {
        document.title = title

        document.querySelectorAll('.tagpage').forEach((p) => {
            p.style.display = 'none'
        });

        document.querySelector(`#${pageID}`).style.display = 'block'
    }

    document.addEventListener("DOMContentLoaded", function () {
        show({{(index .Pages 0 ).ID}}, "{{(index .Pages 0 ).Title}}")
    })
</script>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            {{range $page := .Pages}}
                <a class="nav-item nav-link active"
                   onClick="show('{{$page.ID}}', '{{$page.Title}}')"
                   href="#">{{$page.Title}}</a>
            {{end}}
        </div>
    </div>
</nav>
<div class="container">
    ASDF
    {{range $page := .Pages}}
        <div class="tagpage" id="{{$page.ID}}" style="display: none">{{$page.Body}}</div>
    {{end}}
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"
></script>
</body>
</html>